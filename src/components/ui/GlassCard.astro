---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  interactive?: boolean;
  class?: string;
}
const { as: Tag = "div", interactive = false, class: className = "" } = Astro.props;
---

<Tag class:list={["glass", { "glass-interactive": interactive }, className]}>
  <slot />
</Tag>
